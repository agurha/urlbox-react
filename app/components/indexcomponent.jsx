'use strict';

var React = require('react'),

    Indexcomponent = React.createClass({
      render: function() {
        return (
          <h1 className="Indexcomponent">Welcome to Urlbox React App</h1>
        )
      }
    });

module.exports = Indexcomponent;
